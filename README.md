# hospital-database-management-system
A hospital database management system built with SQLite. Includes relational database design, SQL queries (JOIN, GROUP BY), sample hospital data and Python integration.
